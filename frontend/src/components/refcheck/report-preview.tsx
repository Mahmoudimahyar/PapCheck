"use client";

/** Report preview component (V2 feature stub). */
export function ReportPreview() {
  return (
    <div className="text-sm text-muted-foreground">
      Interactive report preview will be available in V2.
    </div>
  );
}
