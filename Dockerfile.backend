FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml .
COPY src/ src/

RUN pip install --no-cache-dir -e "." && \
    pip install --no-cache-dir uvicorn[standard]

EXPOSE 8001

CMD ["uvicorn", "refcheck.api.main:app", "--host", "0.0.0.0", "--port", "8001"]
